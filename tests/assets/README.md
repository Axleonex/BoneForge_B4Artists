# Test assets

Sample meshes for the behavioural harness are generated **in-host** by
`make_assets.py` (run under Bforartists) so no third-party assets are
committed. `.blend` files here are git-ignored.
